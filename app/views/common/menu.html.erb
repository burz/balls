<% if user_signed_in? %>
  <%= link_to user_path(current_user) do %>
    <header class="menu-header" style="background: url('<%= image_path 'balls-default-icon.jpg' %>') no-repeat 15px 15px;">
      <span class="menu-header-title">
        <%= current_user.name %>
      </span>
    </header>
  <% end %>
  <% if @all_seasons.size > 0 %>
    <div id="log_game_button" class="menu_link">
      <%= link_to new_game_path do %>
        <header>
          <span class="glyphicon glyphicon-book" aria-hidden="true"></span>
          <span class="menu_label">Log game</span>
        </header>
      <% end %>
    </div>
  <% end %>
  <div id="sign_out_button" class="menu_link">
    <%= link_to destroy_user_session_path, method: :delete do %>
      <header>
        <span class="glyphicon glyphicon-log-out" aria-hidden="true"></span>
        <span class="menu_label">Sign out</span>
      </header>
    <% end %>
  </div>
  <section class="menu-section">
    <div class="menu-section-title">
      <%= link_to leagues_path do %>
        Leagues
      <% end %>
      <%= link_to new_league_path, id: 'new_league_button' do %>
        <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
      <% end %>
    </div>
    <ul class="menu-section-list">
      <% for league in @leagues %>
        <li>
          <%= link_to league.name, league_path(league) %>
        </li>
      <% end %>
    </ul>
  </section>
  <section class="menu-section">
    <h3 class="menu-section-title">
      Seasons
    </h3>
    <ul class="menu-section-list">
      <% for season in @all_seasons %>
        <li>
          <%= link_to season.name, league_season_path(season.league, season) %>
        </li>
      <% end %>
    </ul>
  </section>
<% else %>
  <section class="menu-section">
    <h3 class="menu-section-title">
      Please sign in first...
    </h3>
  </section>
<% end %>
