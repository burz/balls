<% if current_user.id == rating.user_id %>
  <tr class="user_row current_user_row"
<% else %>
  <tr class="user_row"
<% end %>
      user_path="<%= user_path rating.user_id %>">
  <td><%= rating.name %></td>
  <td><%= rating.rating %></td>
  <td><%= rating.games_played %></td>
  <td><%= rating.wins %>/<%= rating.losses %></td>
</tr>
