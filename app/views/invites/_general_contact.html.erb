<center>
  <div id="invite_contacts" class="well" leagues_path="<%= url_for leagues_path %>">
    <div class="row">
      <div class="col-md-12">
        <div id="contact_league_alert" class="invite_alert alert alert-danger"
             role="alert" hidden>
          A league must be selected
        </div>
        <div class="form-group">
          <label for"contact_league_selector">League</label>
          <select id="contact_league_selector" class="form-control">
            <option value="">Select a league</option>
            <% for league in @leagues %>
              <option value="<%= league.id %>"><%= league.name %></option>
            <% end %>
          </select>
          <label for="contacts_list">Choose Contacts From Phone</label>&nbsp;
          <ul id="contacts_list" class="list-group">
          </ul>
          <br>
          <button id="send_general_contact_invites" class="btn btn-lg btn-success"
                  type="button">
            Send Invites
          </button>
        </div>
      </div>
    </div>
  </div>
</center>
